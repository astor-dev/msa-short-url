server:
  port: 8092

spring:
  batch:
    jdbc:
      initialize-schema: always
  datasource:
    url: jdbc:mysql://mysql:3306/naverpay?serverTimezone=Asia/Seoul
    username: naverpay
    password: externship
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: redis
      port: 6379
    mongodb:
      uri: mongodb://naverpay:externship@mongodb:27017/naverpay?authSource=admin
  jpa:
    hibernate:
      ddl-auto: update